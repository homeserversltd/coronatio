# PULSE-003 stats data-plane audit

This is the PULSE-003 opening slice for the data-plane projector organ. It scopes only: no behavioral code, no collector, no producer, no projector implementation. The destination is construction from session/topic allowlists, not filtering a full payload after the fact.

Doctrine readback: `coronatio-eternal-appliance-true-form-law` principle 3 requires one session capability membrane and says construction under policy is the law while filtration after construction is heresy (`/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:91-96`). Principle 4 requires data-free push channels and session-projected pulls (`/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:97-100`). Principle 8 requires receipts (`/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:112-113`). The organ liturgy names the audit step and requires ambiguous cells to remain named for rulings (`/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:115-130`).

## A. Current Coronatio stats truth

| row | surface | current truth | real vs stub/readback | citations | scoping consequence |
| --- | --- | --- | --- | --- | --- |
| A1 | `/api/stats` route | Runtime registers `GET /api/stats` to `stats_route`. | Real route. | `src/bands/runtime.rs:69-72`; `src/bands/routes.rs:87-89`. | Existing snapshot route is the pull endpoint to project or wrap in later tranches. |
| A2 | `stats_route()` | Returns `Json(stats_snapshot())`. | Real handler, but only as current snapshot readback; no session field projection yet. | `src/bands/routes.rs:87-89`. | The projector tranche should not add payload to SSE; it should make this pull session/topic-safe or add a same-shape projected endpoint by ruling. |
| A3 | `StatsSnapshot` schema | Snapshot fields are `schema`, `paneId`, `product`, `doctrine`, `transport`, `resources`, `storage`, `network`, `io`, `leases`, `processes`, `services`, `telemetry`, and `nextRoutes`. | Real typed response body. | `src/bands/contracts/core.rs:12-29`. | These fields are the current Coronatio data-plane candidates. |
| A4 | transport posture | Snapshot declares `/api/stats`, `/api/stats/events`, `/api/stats/events/renew`, `streamStatus: available`, and reason `persistent SSE pulse stream and renewal route are registered; pokes carry no stats payload`. | Real readback of current PULSE-001/PULSE-002 posture; not stats producer proof. | `src/bands/crown-law/service-installer.rs:289-295`. | PULSE-003 producer should poke `stats.tick`; the snapshot remains pull. |
| A5 | doctrine/readback fields | Snapshot records preserved sections `cpu-chart`, `network`, `io-section`, `memory`, `disk-usage`, `kea-leases`, `process-usage`, refresh seconds `5`, and read-only authority. | Stub/readback for og viewport provenance and refresh intent, not collector cadence authority. | `src/bands/crown-law/service-installer.rs:271-288`; test wall `src/bands/tests/part-02.rs:238-245`. | Do not confuse pane refresh seconds with og `STATS_INTERVAL` producer cadence. |
| A6 | resource collector | `stats_resources()` reads `/proc/loadavg`, `/proc/meminfo`, and thermal paths; memory/swap are derived from meminfo. | Real first-party readback. | `src/bands/crown-law/service-installer.rs:318-341`; `src/bands/crown-law/service-installer.rs:344-369`; `src/bands/crown-law/service-installer.rs:372-385`. | Eligible raw material for guest projection if field-level placement is allowed. |
| A7 | storage collector | `stats_storage()` shells `df -B1 -P / /home /vault /mnt/nas`, parses bytes/percent/mount, and falls back to a root row with null byte fields when unavailable. | Real local readback with explicit fallback. | `src/bands/crown-law/stats-tabbar.rs:1-41`. | Guest placement for mounts/devices is sensitive enough to keep as an open ruling cell except aggregate posture. |
| A8 | network collector | `stats_network()` reads filtered `/proc/net/dev` interface byte counters and `/proc/net/tcp{,6}` connection counts. | Real local readback; connection counts are aggregate, not per-socket endpoints. | `src/bands/crown-law/stats-tabbar.rs:457-531`. | Interface names and bytes are open-rule fields; connection counts are likely guest-safe aggregates, but still deliberate. |
| A9 | I/O collector | `stats_io()` reads `/proc/diskstats`, maps devices to current storage mounts, and returns cumulative read/write bytes; if no device rows match, it emits zeroed device rows from storage. | Real readback when diskstats matches; fallback rows can be placeholder-like. | `src/bands/crown-law/stats-tabbar.rs:419-455`. | Guest exposure of device/mount labels is open; admin gets full current shape. |
| A10 | Kea lease collector | `stats_kea_leases()` reads `/var/lib/kea/kea-leases4.csv` or `/var/lib/kea/dhcp4.leases`, parses up to 20 `hostname`, `ip`, `mac`, and empty `note`. | Real local readback when files exist; empty if unavailable. | `src/bands/crown-law/stats-tabbar.rs:534-570`; schema `src/bands/contracts/core.rs:129-136`. | MAC/IP/hostname are admin-only until a specific guest LAN visibility ruling exists. |
| A11 | process collector | `stats_processes()` shells `ps -eo comm,pcpu,rss --sort=-pcpu`, skips tool names, and returns up to 10 name/cpu/memory/count rows. | Real local readback; it is currently present in the non-session-projected `/api/stats` surface. | `src/bands/crown-law/stats-tabbar.rs:573-593`; schema `src/bands/contracts/core.rs:138-145`. | This is sensitive and must become admin-only by construction. |
| A12 | service rows | `stats_services()` returns Coronatio as `running` and Caduceus/Harmonia/HomeServer as `readback` routes; privileged systemctl polling is deferred. | Mixed: Coronatio is same-process real; the remaining service statuses are readback-stubs. | `src/bands/crown-law/stats-tabbar.rs:596-615`. | Guest can see stable service summary only if product language needs it; detailed route/status rows remain open/admin. |
| A13 | telemetry and `firstMissingSignal` | Telemetry carries `load1`, CPU temperature, `serviceHealth`, `storagePosture`, and `firstMissingSignal`. Missing signal returns `storage df readback unavailable`, or `service systemctl collector deferred to Caduceus; route readbacks are present`, else `none`. | Real readback of current limitations. | schema `src/bands/contracts/core.rs:156-164`; construction `src/bands/crown-law/service-installer.rs:303-309`; function `src/bands/crown-law/stats-tabbar.rs:617-639`. | Guest should receive `firstMissingSignal` only if product copy can make it non-scary; admin should receive it. |
| A14 | monitor topic law for `system.stats` | Contract table says source `SystemStatsMonitor.broadcast_stats`, cadence source `STATS_INTERVAL`, payload fields `load`, `cpu`, `memory`, `disk`, `network`, `timestamp`, admin fields `processes`, `users`, `networkConnections`, and change rule `always broadcast realtime system stats`. | Recorded contract; collectors/broadcasters are not wired in Coronatio. | `src/bands/crown-law/service-installer.rs:45-50`; PULSE audit `docs/pulse-substrate-audit.md:50-58`. | Producer scoping must use this as cadence/field-law input, not as proof of a live producer. |
| A15 | current collector/broadcaster absence | `service_data_readback()` exposes `monitor_topics` and states `first_missing_live_signal: service collectors and monitor broadcasters are not wired...`. | Explicit recorded-but-unwired state. | `src/bands/crown-law/service-installer.rs:31-42`; PULSE audit `docs/pulse-substrate-audit.md:50-52`. | PULSE-003b must add a producer; this audit does not. |
| A16 | pulse topic enum | `PokeTopic::StatsTick` exists and serializes to `stats.tick`; only `TabsChanged` and `AdminSystem` have proven tests/producers in current source. | Enum-only for stats; no producer found in source. | `src/bands/pulse.rs:12-27`; PULSE-002 tests show `tabs.changed` and `admin.system` pokes, not `stats.tick`: `src/bands/tests/pulse-walls.rs:27-31,41-45`. | Producer tranche must tick/poke `StatsTick` explicitly. |

## B. Og stats data truth (quarry)

| row | quarry fact | exact carried fields | citations | projection consequence |
| --- | --- | --- | --- | --- |
| B1 | Og broadcaster source | `system_stats` registered `SystemStatsMonitor().broadcast_stats` at `app.config['STATS_INTERVAL']`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:426-430`. | Coronatio producer cadence law traces to `STATS_INTERVAL`. |
| B2 | Og cadence value | `STATS_INTERVAL = 1` second. | `/fulcrum/attachments/homeserver/initialization/flask/inject/config.py:102-105`. | First producer should tick once per second unless translator/operator changes the law. |
| B3 | Og monitor wrapper | `broadcast_stats()` returns `collect_stats()`; `broadcast_admin_stats()` currently returns the same collected stats. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/monitors/system.py:12-24,26-37`. | Admin-vs-guest difference was not in the collector; it was in the Socket.IO filter layer. |
| B4 | Top-level tree from `collect_system_stats()` | Top-level keys: `timestamp`, `load_average`, `cpu`, `memory`, `network`, `disk_usage`, `io`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:253-392`. | This is the raw material field universe for `system.stats`. |
| B5 | `load_average` subtree | `load_average.1min`, `load_average.5min`, `load_average.15min`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:210-220,363-366`. | Guest-safe candidate. |
| B6 | `cpu` subtree | `cpu.percent`, `cpu.temp`, `cpu.top_processes`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:356-370`. | `cpu.top_processes` is sensitive and admin-only by construction. |
| B7 | `cpu.top_processes[]` subtree | Per process: `name`, `cpu_percent`, `executablePaths`, `processCount`, `memory_bytes`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:394-484`, especially `:461-467`. | This is the known leak class: executable paths and process inventory must never be present in guest construction. |
| B8 | `memory` subtree | `memory.total`, `memory.used`, `memory.available`, `memory.percent`, `memory.swap.total`, `memory.swap.used`, `memory.swap.free`, `memory.swap.percent`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:371-382`. | Guest-safe candidate as aggregate resource posture. |
| B9 | `network` subtree | `network.sent`, `network.recv`, `network.interfaces`; interfaces include `bytes_recv`, `packets_recv`, `bytes_sent`, `packets_sent` for `tailscale0`, `wan0`, `lan0`, `veth0`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:224-251,272-276,383-387`. | Byte rates are likely guest-safe; interface names and packet totals are open ruling cells. |
| B10 | `disk_usage` subtree | Keys are friendly names such as `root`, `nas`, `nasbackup`; each row carries `total`, `used`, `free`, `percent`, `mountpoint`, `device`, `label`, `configured_path`, optional `dm_device`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:486-574`, especially `:543-557`. | Aggregate usage is guest candidate; mountpoint/device/label/dm_device are sensitive/open. |
| B11 | `io.devices` subtree | Friendly device keys map to `read_bytes`, `write_bytes` rates derived from `psutil.disk_io_counters`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:278-355,389-391`. | Guest may need chartable rates without raw device identity; exact shape is open. |
| B12 | Og declared admin-only denylist for `system_stats` | Registered admin fields: `{'processes', 'users', 'networkConnections'}`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:487-492`. | This was a top-level denylist, not a safe allowlist. |
| B13 | Og filter mechanism | For non-admin SIDs, copied payload and deleted only registered top-level fields if the field existed in the copied dict. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:249-295`, especially `:277-281`. | This missed nested fields and is explicitly quarry. |
| B14 | Actual admin-only after repair | `cpu.top_processes[]` and all nested fields are admin-only. `processes`, `users`, and `networkConnections` remain admin-only contract names if later collectors produce them. | Leak evidence: `docs/pulse-substrate-audit.md:67-75`; quarry field paths above. | Guest payload is built without these fields; no delete pass exists. |
| B15 | Additional sensitive nested fields found | `disk_usage.*.mountpoint`, `disk_usage.*.device`, `disk_usage.*.label`, `disk_usage.*.configured_path`, optional `disk_usage.*.dm_device`; `network.interfaces` names and per-interface packet counters; Kea lease `ip`, `mac`, `hostname` in current Coronatio snapshot. | Og disk fields: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:543-557`; og network interface fields: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:224-246`; current Kea fields: `src/bands/contracts/core.rs:129-136`. | These are not silently placed into guest; they become explicit open/admin rows below. |

## C. Proposed `system.stats` two-projection table

Placement rule: the guest and admin bodies are constructed independently from the raw/current source facts. The admin table is not `full - guest`; it is an explicit second allowlist. `OPEN` means translator/operator ruling required before implementation.

| field path | guest projection | admin projection | reason | citations |
| --- | --- | --- | --- | --- |
| `schema` / `topic` / `timestamp` | ALLOW | ALLOW | Protocol identity and sample time are required for clients. | Og `timestamp`: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:363-365`; current schema: `src/bands/contracts/core.rs:14-29`. |
| `load_average.1min`, `.5min`, `.15min` / current `resources.load.one/five/fifteen` | ALLOW | ALLOW | Aggregate load is core stats-pane data. | Og: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:210-220`; current: `src/bands/crown-law/service-installer.rs:318-341`. |
| `cpu.percent` | ALLOW | ALLOW | Aggregate CPU usage is core stats-pane data. | Og: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:267-268,366-370`. |
| `cpu.temp` / current `resources.load.cpuTemperatureCelsius` | ALLOW | ALLOW | Aggregate temperature is appliance health data. | Og: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:270,366-370`; current: `src/bands/crown-law/service-installer.rs:372-385`. |
| `cpu.top_processes[]` | DENY | ALLOW | Process inventory and executable paths are the known nested leak class. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:394-484`; `docs/pulse-substrate-audit.md:67-75`. |
| `cpu.top_processes[].name` | DENY | ALLOW | Process identity. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:461-467`. |
| `cpu.top_processes[].cpu_percent` | DENY | ALLOW | Meaningful only with process identity; do not leak row count/timing. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:461-467`. |
| `cpu.top_processes[].executablePaths` | DENY | ALLOW | Executable paths are sensitive host filesystem/process data. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:415-422,461-467`. |
| `cpu.top_processes[].processCount` | DENY | ALLOW | Process grouping cardinality. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:461-467`. |
| `cpu.top_processes[].memory_bytes` | DENY | ALLOW | Process memory ties to process identity. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:434-467`. |
| `memory.total/used/available/percent` | ALLOW | ALLOW | Aggregate memory health. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:371-375`; current schema `src/bands/contracts/core.rs:67-74`. |
| `memory.swap.total/used/free/percent` | ALLOW | ALLOW | Aggregate swap health. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:376-381`. |
| `network.sent`, `network.recv` | ALLOW | ALLOW | Aggregate rates drive the public network chart. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:272-276,383-385`. |
| `network.interfaces[].name` | OPEN | ALLOW | Interface names can reveal topology (`tailscale0`, `wan0`, `lan0`, `veth0`). Decide whether guest chart needs named rows or only aggregate WAN/total. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:224-246`. |
| `network.interfaces[].bytes_recv/bytes_sent` | OPEN | ALLOW | Guest chart may need per-interface totals, but topology naming is coupled unless transformed. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:224-246`; current `src/bands/crown-law/stats-tabbar.rs:464-490`. |
| `network.interfaces[].packets_recv/packets_sent` | OPEN | ALLOW | Packet counts are not currently in Coronatio snapshot and may not be needed. | Og: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:241-246`; current omits packets: `src/bands/contracts/core.rs:95-102`. |
| current `network.connections.established/listening/total` | OPEN | ALLOW | Aggregate connection counts replaced og `networkConnections` idea but can expose service posture; needs ruling. | `src/bands/contracts/core.rs:104-110`; `src/bands/crown-law/stats-tabbar.rs:503-531`. |
| `disk_usage.*.total/used/free/percent` / current `storage[].totalBytes/usedBytes/freeBytes/usagePercent` | ALLOW as aggregate rows if label sanitized | ALLOW | Disk usage chart is public product posture; raw mount/device names are separate. | Og: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:537-548`; current schema `src/bands/contracts/core.rs:76-86`. |
| `disk_usage.*.mountpoint`, `.device`, `.label`, `.configured_path`, `.dm_device` / current `storage[].name`, `storage[].mount`, `source` | OPEN / default DENY until ruled | ALLOW | Raw mount/device/configured path names can reveal topology. Guest may receive product labels only if constructed separately. | Og: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:543-557`; current: `src/bands/crown-law/stats-tabbar.rs:1-41`. |
| `io.devices.*.read_bytes/write_bytes` / current `io.devices[].readBytes/writeBytes` | OPEN as aggregate/chart series | ALLOW | Chart may be guest-safe if device identity is abstracted; current shape couples device+mount. | Og: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:278-355,389-391`; current: `src/bands/crown-law/stats-tabbar.rs:419-455`. |
| current `io.devices[].device`, `.mount` | OPEN / default DENY until ruled | ALLOW | Device/mount topology. | `src/bands/contracts/core.rs:113-126`; `src/bands/crown-law/stats-tabbar.rs:419-455`. |
| current `leases[].hostname/ip/mac/note` | DENY | ALLOW | DHCP lease identity and MAC/IP inventory are admin data absent a guest LAN ruling. | `src/bands/contracts/core.rs:129-136`; `src/bands/crown-law/stats-tabbar.rs:534-570`. |
| current `processes[]` | DENY | ALLOW | Current Coronatio process rows are the repaired equivalent of the og leak class and must not be guest. | `src/bands/contracts/core.rs:138-145`; `src/bands/crown-law/stats-tabbar.rs:573-593`. |
| current `services[].name/status/details/route` | OPEN summary for guest; full rows admin | ALLOW | Coronatio row is real, others are route readback stubs; customer surface may need safe currentness, not raw diagnostic route details. | `src/bands/crown-law/stats-tabbar.rs:596-615`. |
| `telemetry.load1`, `telemetry.cpuTemperatureCelsius`, `telemetry.storagePosture` | ALLOW | ALLOW | Aggregated appliance posture. | `src/bands/contracts/core.rs:156-164`; `src/bands/crown-law/service-installer.rs:303-309`. |
| `telemetry.serviceHealth`, `telemetry.firstMissingSignal` | OPEN | ALLOW | `firstMissingSignal` currently contains implementation/deferred-collector language; guest may need human appliance copy instead. | `src/bands/crown-law/stats-tabbar.rs:617-639`. |
| `nextRoutes.snapshot/events/renew` and `transport.*` | ALLOW public route names | ALLOW | Existing client requires routes; no secret data. | `src/bands/crown-law/service-installer.rs:289-295,310-314`. |

## D. Producer scoping

| row | producer need | scope | citations | implementation note |
| --- | --- | --- | --- | --- |
| D1 | Cadence | `system.stats` producer ticks at og `STATS_INTERVAL`; quarry value is 1 second. | Monitor law: `src/bands/crown-law/service-installer.rs:49`; quarry value: `/fulcrum/attachments/homeserver/initialization/flask/inject/config.py:102-105`; quarry registration: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:426-430`. | Implement as a runtime ticker, not as client polling. |
| D2 | Pulse event | Use existing `PokeTopic::StatsTick -> stats.tick`. | `src/bands/pulse.rs:12-27`. | Do not carry stats data in the frame; `poke_frame()` emits `{}`. |
| D3 | Poke policy | First pass is poke-on-tick, not predicate-on-change. | PULSE audit P3 settled no-predicates-first-pass: `docs/pulse-substrate-audit.md:91`; current pulse `poke_frame` is data-free: `src/bands/pulse.rs:267-272`. | Later predicate work can compare projected/raw state, but not in PULSE-003b. |
| D4 | Runtime residence | Ticker belongs with the runtime/bus startup seam so one process owns the interval and calls `pulse::poke(PokeTopic::StatsTick)`. | Runtime routes own `/api/stats/events` and `/api/stats`: `src/bands/runtime.rs:69-72`; pulse bus is process-global `OnceLock`: `src/bands/pulse.rs:34-62`. | Recommended tranche starts runtime ticker with idempotent guard beside app construction, not per request. |
| D5 | Collector boundary | Producer does not compute/send payload. It only invalidates; the subsequent client pull constructs session projection. | True-form principle 4: `/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:97-100`; current `stats_route()` pull: `src/bands/routes.rs:87-89`. | Keeps pulse stable and projection testable. |

## E. Client scoping

| row | current client truth | citation | PULSE-003 client consequence |
| --- | --- | --- | --- |
| E1 | Stats pane shell contains `__STATS_ELEMENTS_FRAGMENT__` inside `data-stats-viewport`. | `src/bands/shell/document-2.rs:127-130`; replacement: `src/bands/shell/render.rs:119-122`. | The session-projected fragment is already the native replacement target. |
| E2 | `/api/stats/elements` serves `render_stats_elements_fragment(session)` with `no-store` and CSP. | `src/bands/full-rust-routes.rs:3-7`; `src/bands/crown-law/element-fragments.rs:39-60`. | Stats.tick should re-fetch this same fragment or the stats-data fragment chosen by ruling. |
| E3 | VIS-003 stats elements are seven element fragments: `cpu-chart`, `network-chart`, `io-section`, `memory-usage`, `disk-usage`, `kea-leases`, `process-list`. | `src/bands/crown-law/element-fragments.rs:67-110`; test wall `src/bands/tests/part-02.rs:265-287`. | Field projection must align to these element ids; process-list and Kea rows are admin-only by construction. |
| E4 | Current admin/session changes call `refreshElementFragment('stats')`, which fetches `/api/stats/elements`, swaps the viewport HTML, hydrates stats, and reapplies admin mode. | `src/bands/shell/document-3.rs:50-61`; `src/bands/shell/document-4.rs:529-541`. | `stats.tick` should use the same pull-to-project shape, not a bespoke payload listener. |
| E5 | PULSE-002 client listens to `tabs.changed` and re-fetches tab bar only. It does not listen to `stats.tick` today. | `src/bands/shell/document-3.rs:468-486`; test wall `src/bands/tests/pulse-walls.rs:247-253`. | Client tranche adds a `stats.tick` listener that re-fetches session-projected stats content. |
| E6 | Element visibility writes already re-render the affected stats/portals fragment through the server. | `src/bands/shell/document-4.rs:542-556`; routes `src/bands/full-rust-routes.rs:3-7`. | Data-plane projector should not bypass this fragment membrane. |

## F. Proposed implementation tranche plan

| tranche | wall | change set | proof | stop/yield condition |
| --- | --- | --- | --- | --- |
| PULSE-003b | Producer only | Add idempotent runtime ticker for `PokeTopic::StatsTick` at `STATS_INTERVAL` law; no payload; no projection changes. | Unit/wall proving `stats.tick` appears after tick and SSE data is `{}`; existing pulse walls remain green. | Publish producer-only slice. |
| PULSE-003c | Projector model only | Add pure `system.stats` raw-to-projection function with explicit guest/admin allowlist structs/tables; no routes/client wiring. | Exhaustive wall: guest projection contains only allowlisted fields and cannot contain `top_processes`, `processes`, `users`, `networkConnections`, `executablePaths`, Kea MAC/IP/hostname, raw mount/device paths. | Publish pure organ slice. |
| PULSE-003d | `/api/stats` projection membrane | Wire stats pull through session membrane using PULSE-003c projector or add a named projected stats fragment endpoint if translator chooses separate route. | Route tests for guest/admin response field presence/absence; proof that guest body is constructed without denied fields. | Publish route-only slice. |
| PULSE-003e | Client stats.tick rider | Add `stats.tick` EventSource listener that re-fetches the session-projected stats fragment/payload using existing `refreshElementFragment('stats')` pattern or the ruled endpoint. | JS wall and browser/DOM proof: `stats.tick` causes pull, not payload read; guest/admin projections remain different. | Publish client-only slice. |
| PULSE-003f | Collector fidelity follow-up | Replace or extend current `/api/stats` readback-stub fields with og-equivalent collectors only where safe and ruled. | Collector tests with fixtures for `/proc`, `df`, Kea, ps, diskstats; no public raw topology leaks. | Publish only after open field rulings close. |
| PULSE-003g | Predicate/efficiency optional | If later needed, add change predicates after first-pass no-predicate law. | Predicate wall comparing constructed projections, not full sensitive payloads. | Only after operator requests efficiency/predicate work. |

## G. Open ruling cells

| id | ruling needed | default until ruled | source pressure |
| --- | --- | --- | --- |
| O1 | Should `/api/stats` itself become session-projected, or should a new fragment/data endpoint carry projected stats while `/api/stats` remains admin/readback? | Do not guess; recommended default is make the existing pull endpoint session-projected because it is the stats pane state route. | Current route is `/api/stats` (`src/bands/routes.rs:87-89`); native pane points to it (`src/bands/tests/part-02.rs:255-262`). |
| O2 | Guest visibility for `network.interfaces[].name` and per-interface bytes. | Deny raw names; allow aggregate network rates. | Og named interfaces include `tailscale0`, `wan0`, `lan0`, `veth0` (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:224-246`). |
| O3 | Guest visibility for connection counts. | Deny or hide behind product summary until ruled. | Current Coronatio adds aggregate TCP counts (`src/bands/crown-law/stats-tabbar.rs:503-531`); og denylist named `networkConnections` (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:487-492`). |
| O4 | Guest disk labels/mounts/devices. | Guest receives aggregate usage with product labels only; raw mount/device fields admin-only. | Og/current fields expose mountpoint/device/source (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:543-557`; `src/bands/contracts/core.rs:76-86`). |
| O5 | Guest I/O chart shape. | Guest may receive chartable aggregate series only if decoupled from raw device/mount names. | Current `StatsIoDevice` couples device/mount/read/write (`src/bands/contracts/core.rs:113-126`). |
| O6 | Guest `services` and `telemetry.firstMissingSignal` language. | Guest gets appliance-safe currentness, not implementation/deferred-collector strings. | Current missing signal says Caduceus/systemctl collector deferred (`src/bands/crown-law/stats-tabbar.rs:617-623`). |
| O7 | Whether `kea-leases` remains visible as a guest element shell when its data is admin-only. | Keep shell visibility under VIS/iris; guest projection returns empty/unavailable data rather than MAC/IP/hostname. | VIS-003 element list includes `kea-leases` (`src/bands/crown-law/element-fragments.rs:98-100`). |
| O8 | Whether current Coronatio `processes[]` should be moved under admin-only topic `admin.system` or remain admin field of `system.stats` for admin projection. | Admin projection may include it; guest never receives it. | Monitor law has `system.stats` admin fields and separate `admin.system` topic (`src/bands/crown-law/service-installer.rs:49,56`). |

## H. Mechanical inventory

- Current Coronatio stats truth rows: 16.
- Og stats quarry rows: 15.
- Projection placement rows: 27.
- Producer scoping rows: 5.
- Client scoping rows: 6.
- Proposed implementation tranches: 6.
- Open ruling cells: 8.

This audit stops at scope. It leaves the implementation organ walled into publishable slices and keeps ambiguous field placement visible instead of silently resolving it.
