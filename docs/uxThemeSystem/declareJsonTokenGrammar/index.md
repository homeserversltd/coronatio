# DeclareJsonTokenGrammar

Keep the flat JSON map as the compatibility membrane. Add names by intent: roles, surfaces, outlines, state-layer opacities, highlight layers, accent families, gradients, elevation, overlays, focus, contrast, density, component slots, and explicit feature flags. Values stay strings so the current route and CSS projection keep working.
