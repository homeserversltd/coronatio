# PULSE-000 pulse substrate audit

This artifact opens the PULSE organ campaign. It is an audit seed for PULSE-001: citation-true broadcast archaeology, current Coronatio pulse/stub inventory, and rulings only. No stream bus, poke bus, subscriber registry, or projector implementation is designed here.

Doctrine readback used: `coronatio-eternal-appliance-true-form-law` says organ campaigns begin with an audit table, name ambiguous cells, and leave operator rulings explicit (`/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:115-130`). Its true-form principle 4 requires data-free push channels and session-projected reads (`/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:97-100`). The sibling vocabulary names Rust as state machine, Maud as mold, SSE as pulse, and the client projector as blitter (`/fulcrum/attachments/paligenesis/docs/infrastructure/arcadia-living-state-projection-law.md:31-38,56-59`). VIS-000 is the admitted rigor exemplar and already records PUSH-TO-POKE/PULL-TO-PROJECT for the visibility lane (`docs/iris-transition-table.md:41-45`).

## A. Governing rulings

| id | ruling | citations |
| --- | --- | --- |
| R1: pulse is poke-only | Coronatio true-form pulse events SHALL carry invalidation pokes, not product payloads. A client that receives a poke re-fetches through its own session membrane and receives its own projection. | True-form principle 4: `/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:97-100`; VIS-000 ruling: `docs/iris-transition-table.md:41-45`. |
| R2: per-socket payload filtering dies | Og filtered payloads per SID at emit time. The pulse inversion makes per-socket filtering cease to exist as a category; projection happens by session-specific pull/allowlist construction. | Og filter: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:249-295,355-391`; true-form construction-under-policy: `/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:91-100`. |
| R3: security bug divergence | Og security defects are approved divergence. The nested process leak is documented below and SHALL NOT be ported. | True-form security divergence: `/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:51-60`; VIS-000 bug-fidelity row: `docs/iris-transition-table.md:46-47,203-204`. |
| R4: audit before organ | This document is only the audit step. PULSE-001 may build against these rows, but this slice does not design or implement the stream/poke bus. | Organ liturgy: `/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:115-130`. |

## B. Og Socket.IO broadcast model

| row | og behavior | citation-true finding | ruling / PULSE consequence |
| --- | --- | --- | --- |
| B1 | Broadcast manager owns per-topic subscribers, broadcaster functions, intervals, connected SIDs, initialized subscriber sets, admin-only topic set, and admin-field registries. | `BroadcastManager.__init__` initializes `subscribers`, `events`, `broadcasters`, `intervals`, `connected_sids`, `initialized_subscribers`, `admin_only_broadcasts`, and `broadcasts_with_admin_fields` (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:26-43`). | PULSE-001 starts from explicit Coronatio topic/poke state, not from implicit Socket.IO manager bags. |
| B2 | Client subscribe request is Socket.IO `subscribe`; it reads `data.type`, ignores duplicates, then calls `broadcast_manager.add_subscriber`. | Socket handler: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/sockets/events.py:362-386`; subscription confirmation: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/sockets/events.py:387-393`. | Socket.IO subscribe is quarry. Current Coronatio law records SSE EventSource plus POST renew as the replacement transport (`src/bands/crown-law/registry-startup.rs:481`). |
| B3 | Client unsubscribe request is Socket.IO `unsubscribe`; it reads `data.type`, calls `broadcast_manager.remove_subscriber`, and emits an unsubscribe confirmation. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/sockets/events.py:405-425`; manager removal discards from subscribers and initialized sets (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:114-127`). | SSE lifecycle/lease replaces subscribe/unsubscribe diffing; no Socket.IO lifecycle ports. |
| B4 | Admin-only channels reject non-admin subscriptions at subscription time. | `add_subscriber` checks `broadcast_type in self.admin_only_broadcasts`, validates the SID, and returns without adding on failure (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:62-80`). | True form does not grant admin payload by subscribing; session membrane controls subsequent pull/projection. |
| B5 | Admin-only channel subscribers are revalidated before each emit tick. | Generic broadcaster revalidates `admin_only_broadcasts` SIDs and skips stale/non-admin SIDs (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:355-363`). | True-form equivalent is every pull through the session membrane; no trusted long-lived payload entitlement on the wire. |
| B6 | Mixed channels register admin-only fields by broadcast type. | `register_admin_fields` stores field sets (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:157-163`); init registers `internet_status`, `vpn_status`, `system_stats`, `tailscale_status`, and `services_status` field families (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:487-492`). | PULSE records this as quarry truth; the true-form data plane must later project by allowlist construction. |
| B7 | Mixed channel filtering is per-SID and delete-by-denylist. | `filter_admin_data` copies the payload, validates socket admin status, and for non-admin SIDs deletes top-level registered fields before returning the payload (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:249-295`). | Approved death-row mechanism: per-SID payload filtering does not port. |
| B8 | Generic broadcaster emits per-SID after optional filtering and per-SID `should_broadcast`. | It fetches data, loops subscribers, applies `filter_admin_data` when admin fields exist, calls `should_broadcast`, and emits to `room=sid` (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:330-391`). | PULSE-001 may preserve change/invalidation semantics, but not payload delivery/filtering. |
| B9 | Immediate broadcasts use the same admin-only and admin-field path. | `trigger_immediate_broadcast` fetches broadcaster data, validates admin-only SIDs, filters mixed-channel data per SID, emits to each room, and stores `last_broadcast_data` (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:298-325`). | Any future immediate path is a data-free poke only. |
| B10 | Broadcasters are registered with Socket.IO topic names and intervals; admin disk/system are admin-only. | `init_broadcasters` registers `system_stats`, `services_status`, `power_status`, `internet_status`, `tailscale_status`, `vpn_status`, `hard_drive_test_status`, `sync_status`, then admin-only `admin_system` and `admin_disk_info` (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:420-485`). | Coronatio topic contracts may rename/canonicalize these into dot-topic law; see section D. |
| B11 | Tab visibility writes pushed unfiltered `visibility_updated` to all sockets. | `/api/tabs/visibility` writes config then calls `socketio.emit('visibility_updated', {'tabId': tab_id, 'visibility': visibility})` without room/session filter (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/tabman/routes.py:199-214`). | In true form this becomes a poke; each client pulls its session-safe tab-bar/plan projection. |
| B12 | Element visibility writes pushed unfiltered `element_visibility_updated` to all sockets. | `/api/tabs/elements` writes config then emits `element_visibility_updated` with `tabId`, `elementId`, and `visibility` to all sockets (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/tabman/routes.py:259-281`). | Same poke-only inversion as B11. |
| B13 | Starred-tab writes pushed unfiltered `starred_tab_updated` to all sockets. | Fallback star emits `{'tabId': 'fallback'}` (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/tabman/routes.py:93-114`); normal star emits `{'tabId': tab_id}` (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/tabman/routes.py:127-151`). | Same poke-only inversion as B11. |

## C. Current Coronatio stub inventory

| surface | current Coronatio truth | citation | ruling / status |
| --- | --- | --- | --- |
| `/api/stats/events` route registration | The runtime registers `GET /api/stats/events` to `stats_events_route`. | `src/bands/runtime.rs:70`. | Present route, stubbed stream. |
| `stats_events_route()` | Builds one `stats_event_payload`, serializes it into exactly one SSE frame with `event: stats.system`, `id`, and `data`, then returns an Axum response. | `src/bands/router-readback.rs:25-39`. | One frame then close; no loop, registry, notify, or broadcast. |
| SSE response headers | The route sets `Content-Type: text/event-stream`, `Cache-Control: no-cache`, and `x-coronatio-schema: coronatio.stats.events.v1`. | `src/bands/router-readback.rs:32-38`. | Header shape exists; live stream mechanics do not. |
| `/api/stats/events/renew` route registration | The runtime registers `POST /api/stats/events/renew` to `stats_events_renew_route`. | `src/bands/runtime.rs:70-71`. | Present lease-readback route, not enforced. |
| `stats_events_renew_route()` | Returns JSON schema `coronatio.stats.events.renewal.v1`, topic `stats.system`, route, `lease_seconds: 30`, status `renewed-contract`, and next renewal before 20 seconds. | `src/bands/router-readback.rs:41-50`. | 30-second lease contract exists as readback only; no stream expiry enforcement exists here. |
| `/api/monitor/pulse` route registration | The runtime registers `GET /api/monitor/pulse` to `monitor_pulse_route`. | `src/bands/runtime.rs:59-61`. | Present monitor readback route. |
| `monitor_pulse_route()` | Returns `monitor_pulse_readback()` as JSON. | `src/bands/router-readback.rs:5-7`. | Readback stub only. |
| `monitor_pulse_readback()` | Readback names schema `coronatio.monitor-pulse.v1`, snapshot/event/renew routes, first event, and proof policy strings including initial state, change predicate, admin filtering, and SSE heartbeat/expiry replacement. | `src/bands/crown-law/session-topics-frontend.rs:124-138`. | Contract text exists; no broadcast implementation is built. |
| `stats_event_payload()` | Readback payload is schema `coronatio.stats.event.v1`, topic `stats.system`, event id `stats-system-bootstrap-1`, event `snapshot`, `lease_seconds: 30`, payload state `snapshot-available`, and first missing signal. | `src/bands/crown-law/session-topics-frontend.rs:141-150`; struct fields: `src/bands/contracts/constants.rs:477-485`. | This is a payload-bearing bootstrap stub, not the final data-free poke law. PULSE ruling R1 governs the future organ. |
| Recorded transport law | Topic catalog records `SSE EventSource plus POST renew; Socket.IO subscribe/unsubscribe is quarry only`. | `src/bands/crown-law/registry-startup.rs:478-483`. | Recorded law, not live stream substrate. |
| Negative space | `grep -Rn 'text/event-stream\|EventSource\|Sse::\|broadcast::\|tokio::sync' src/` in the PULSE-000 worktree returns only `router-readback.rs:34`, `registry-startup.rs:481`, and tests asserting those strings. | Source hits: `src/bands/router-readback.rs:34`; `src/bands/crown-law/registry-startup.rs:481`; `src/bands/tests/part-03.rs:470`; `src/bands/tests/part-04.rs:40`. | No source subscriber registry, notify bus, stream loop, `Sse::`, `broadcast::`, or `tokio::sync` lane exists. |

## D. Coronatio `monitor_topic_laws()` table

The table below reproduces the current topic contract table faithfully. It is contract-only today: `service_data_readback()` exposes `monitor_topics: monitor_topic_laws()` and states the first missing live signal is that collectors and monitor broadcasters are not wired (`src/bands/crown-law/service-installer.rs:31-42`).

| topic | source monitor | cadence source | payload fields | admin_only | admin_fields | change rule | Coronatio contract | citation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `services.status` | `ServicesMonitor.broadcast_status` | `SERVICES_CHECK_INTERVAL` | `name`, `systemdName`, `isActive`, `status`, `statusDetails`, `isScriptManaged`, `port`, `needsReboot` | false | `isEnabled` | broadcast when service count/name/status/enabled state changes | service cards and portal currentness readback | `src/bands/crown-law/service-installer.rs:45-49`. |
| `power.status` | `PowerMonitor.broadcast_power_data` | `POWER_SAMPLE_INTERVAL` | `current`, `historical`, `unit`, `timestamp` | false | none | always broadcast realtime power samples | power indicator/currentness readback | `src/bands/crown-law/service-installer.rs:47-49`. |
| `system.stats` | `SystemStatsMonitor.broadcast_stats` | `STATS_INTERVAL` | `load`, `cpu`, `memory`, `disk`, `network`, `timestamp` | false | `processes`, `users`, `networkConnections` | always broadcast realtime system stats | stats pane snapshot/SSE payload | `src/bands/crown-law/service-installer.rs:49`. |
| `internet.status` | `InternetStatusMonitor.broadcast_status` | `INTERNET_CHECK_INTERVAL` | `status`, `timestamp` | false | `publicIp`, `ipDetails`, `dnsServers` | broadcast on connectivity/public IP/DNS/error-validity changes | internet indicator readback | `src/bands/crown-law/service-installer.rs:50`. |
| `tailscale.status` | `TailscaleMonitor.broadcast_status` | `TAILSCALE_CHECK_INTERVAL` | `status`, `interface`, `timestamp` | false | `ip`, `tailnet`, `isEnabled`, `loginUrl` | broadcast on status/interface/admin-field/login URL changes | tailscale indicator readback | `src/bands/crown-law/service-installer.rs:51`. |
| `vpn.status` | `VPNMonitor.broadcast_status` | `VPN_CHECK_INTERVAL` | `vpnStatus`, `transmissionStatus`, `timestamp` | false | `connectionDetails`, `credentials`, `isEnabled` | broadcast on vpn/transmission/enabled changes | vpn indicator readback with credentials redacted | `src/bands/crown-law/service-installer.rs:52`. |
| `sync.status` | `sync_monitor.broadcast_status` | `2 seconds` | `status`, `id`, `progress`, `timestamp`, `message` | false | none | broadcast on status/job/progress >=10% or working keepalive | sync indicator and admin action readback | `src/bands/crown-law/service-installer.rs:53`. |
| `hard-drive-test.status` | `HardDriveTestMonitor.broadcast_status` | `DRIVE_TEST_INTERVAL` | `status`, `device`, `testType`, `progress`, `message`, `timestamp` | false | none | broadcast starting/done and working keepalive | drive-test indicator/action readback | `src/bands/crown-law/service-installer.rs:54`. |
| `admin.disk.info` | `DiskMonitor.broadcast_disk_info` | `DISK_CHECK_INTERVAL` | `blockDevices`, `diskUsage`, `encryptionInfo`, `nasCompatibleDevices`, `timestamp` | true | none | broadcast on device/encryption/NAS/mount/filesystem/UUID/usage/error changes or 5 minute pulse | admin disk appliance readback | `src/bands/crown-law/service-installer.rs:55`. |
| `admin.system` | `SystemStatsMonitor.broadcast_admin_stats` | `ADMIN_STATS_INTERVAL` | `system stats plus admin detail fields` | true | none | admin-only realtime system pulse | admin diagnostics readback | `src/bands/crown-law/service-installer.rs:56`. |

## E. Og nested-leak bug: approved divergence, do not port

| mechanism | exact finding | citations | ruling |
| --- | --- | --- | --- |
| Admin fields for `system_stats` were top-level denylist keys. | Og registered `system_stats` admin fields as `{'processes', 'users', 'networkConnections'}`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:487-492`. | This denylist did not name nested `cpu.top_processes`. |
| Non-admin filtering deleted only matching top-level keys. | `filter_admin_data` copies the dict and deletes a registered field only when `field in filtered_data`; it does not traverse nested maps. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:249-295`. | Nested admin-sensitive values survive this filter. |
| `collect_system_stats()` placed process data under `cpu.top_processes`. | It calls `top_processes = collect_process_stats()` and returns it nested at `cpu.top_processes`. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:356-370`. | The registered top-level key `processes` never matched this nested path. |
| The nested process list contains process names, executable paths, process counts, CPU, and memory fields. | `collect_process_stats()` appends `name`, `cpu_percent`, `executablePaths`, `processCount`, and `memory_bytes`, then returns the top 10 list. | `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:445-484`. | Leak is concrete, not theoretical. |
| Named ruling | Approved divergence: true-form data plane MUST project by allowlist construction, never delete by denylist. Security-class og defects are not ported. | True-form law: `/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:51-60,91-100`; VIS-000 death-row line: `docs/iris-transition-table.md:203-204`. | PULSE-001 must not recreate per-socket delete filters. |

## F. Poke-vs-payload governing inversion

| og model | true-form pulse inversion | citations |
| --- | --- | --- |
| Og pushed full data payloads over Socket.IO topics, then filtered mixed topics per SID and skipped admin-only SIDs at emit time. | True form pushes data-free invalidation pokes over SSE; every client then pulls a session-projected payload through the request membrane. | Og emit/filter: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:355-391`; true-form principle 4: `/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:97-100`; VIS-000: `docs/iris-transition-table.md:41-45`. |
| Og visibility/star changes emitted payload fragments (`tabId`, `elementId`, `visibility`) globally to all sockets. | True form emits a poke only; clients re-fetch `iris`/projected fragments under their own session. | Og tab emits: `/fulcrum/attachments/homeserver/initialization/flask/inject/backend/tabman/routes.py:111-151,213-214,276-281`; IRIS construction/poke law: `docs/iris-transition-table.md:38-45`. |
| Og mixed-channel filter was delete-by-denylist and therefore missed nested `cpu.top_processes`. | True-form projection is allowlist construction by topic/session; fields not granted are never constructed for that session payload. | Og leak citations in section E; construction-under-policy law: `/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:91-100`. |

## G. Ambiguous cells / open operator rulings

| id | ambiguity | source finding | status needed |
| --- | --- | --- | --- |
| P1 | Canonical pulse topic names for visibility/star pokes are not yet declared in `monitor_topic_laws()`. | Current topic table covers service/power/system/internet/tailscale/vpn/sync/drive/admin topics only (`src/bands/crown-law/service-installer.rs:45-56`); og visibility events are `visibility_updated`, `element_visibility_updated`, and `starred_tab_updated` (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/tabman/routes.py:111-151,213-214,276-281`). | SETTLED 2026-07-06: `PokeTopic::TabsChanged` is the one visibility/star/element pulse topic; wire event `tabs.changed`; clients re-project everything tab-shaped on one poke. Enum may name future `StatsTick -> stats.tick` and `AdminSystem -> admin.system` without producers. |
| P2 | Current `stats_event_payload()` is payload-bearing bootstrap readback, while true-form pulse law says data-free poke. | Current payload struct includes schema/topic/event id/event/lease/payload state/first missing signal (`src/bands/crown-law/session-topics-frontend.rs:141-150`; `src/bands/contracts/constants.rs:477-485`); true-form says push channels carry data-free pokes (`/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:97-100`). | SETTLED 2026-07-06: `stats_event_payload()` and the one-frame `event: stats.system` bootstrap retire in PULSE-001. The stats data plane returns in PULSE-003 as session-projected pull allowlist construction, never as pulse payload. |
| P3 | Change predicate authority is recorded, but the owning predicate bodies for true-form topics are not yet enumerated. | Coronatio records `change_rule` strings in `monitor_topic_laws()` (`src/bands/crown-law/service-installer.rs:45-56`) and og has `should_broadcast` in the generic emit path (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:374-391`). | SETTLED 2026-07-06: first organ pass has no predicates; future mutation sites poke unconditionally. Predicate law is deferred to the PULSE-003 data plane. |
| P4 | Lease expiry behavior is recorded as law but unenforced in current source. | Topic catalog says expired streams produce an expired event and close (`src/bands/crown-law/registry-startup.rs:481-483`); renew route only returns JSON (`src/bands/router-readback.rs:41-50`). | SETTLED 2026-07-06: a stream hands the client a stream id in its first metadata frame; `POST /api/stats/events/renew?streamId=<id>` renews that connected stream; an unrenewed stream emits `pulse.expired` and closes at the recorded 30s lease. Expiry SHALL re-check the current lease when the sleep wakes, so a quiet stream parked in the wait observes lawful renewals before emitting `pulse.expired`. |

## H. Mechanical inventory for PULSE-001

- Og Socket.IO model rows: 13.
- Current Coronatio stub rows: 10.
- Monitor topic contract rows: 10.
- Security divergence rows: 5.
- Open operator ruling cells: 0 (P1-P4 settled 2026-07-06 for PULSE-001).

This document intentionally stops at the audit membrane. It records quarry truth, present Coronatio truth, named rulings, and ambiguous cells for operator/translator settlement before PULSE-001 builds the stream + poke bus.
