# FIELD-000 field projection audit

This is the FIELD-000 docs-only audit for per-field privilege projection in Coronatio. It creates no source, Cargo, or wall changes. Its job is to map every guest-reachable fact route, classify mixed fields by the five fact-kinds, and name the ruling table that lets delete-by-denylist die.

## A. Method and governing law recap

Method: read the live Coronatio route table and handlers at this worktree head; read the already-admitted stats/pulse audit documents; read the Paligenesis governing leaves; then classify only facts that are served to guest-reachable surfaces. Mutation-only routes are listed where they return fact readbacks or are in the operator-provided route census; `/api/admin/*` is treated as one whole-route admin class.

Governing law: the true-form leaf defines the five fact-kinds (`identity`, `lifecycle`, `privilege`, `preference`, `secret`) and requires one session membrane with construction under policy, not filtration after construction (`/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:62-77`, `/fulcrum/attachments/paligenesis/docs/workflow/coronatio-eternal-appliance-true-form-law.md:91-100`). The pulse leaf says pokes carry nothing and every read is a session-projected pull (`/fulcrum/attachments/paligenesis/docs/workflow/workflow-coronatio-pulse-organ-poke-substrate-law.md:26-44`, `/fulcrum/attachments/paligenesis/docs/workflow/workflow-coronatio-pulse-organ-poke-substrate-law.md:74-82`). The stats route is already lawful: `/api/stats` chooses `project_system_stats_admin()` or `project_system_stats_guest()` from the session membrane (`src/bands/routes.rs:87-93`), and the guest type cannot represent denied fields (`src/bands/contracts/core.rs:31-42`, `src/bands/contracts/core.rs:109-145`).

## B. Route census table

Transport terms: `snapshot pull` means request/response read. `pulse-riding pull` means the route is refreshed after a data-free poke.

| id | guest-reachable fact route(s) | handler file:line | current session behavior | transport | citation |
| --- | --- | --- | --- | --- | --- |
| B1 | `/api`, `/api/panes`, `/api/panes/:pane_id`, `/api/registry`, `/api/registry/transaction`, `/api/startup`, `/api/lanes`, `/api/fallback`, `/api/boundary`, `/api/installer`, `/api/faults`, `/api/topics`, `/api/monitor/pulse`, `/api/services/data`, `/api/frontend/storage` | `src/bands/routes.rs:24-77`, `src/bands/runtime.rs:60-74` | guest-only-by-nature readback/contract facts; no field projection law beyond current readback shape | snapshot pull | root lists route facts and native panes (`src/bands/routes.rs:24-77`); runtime mounts these API routes (`src/bands/runtime.rs:60-74`). |
| B2 | `/api/session`, `/api/validatePin`, `/api/verifyPin`, `/api/logout`, `/api/admin/ping` | `src/bands/runtime.rs:40-43`, `src/bands/router-readback.rs:353-445` | session membrane; `/api/validatePin` returns a token only after PIN verification; session detection maps valid `X-Admin-Token` to `Session::Admin` else guest | snapshot pull | token header and authorization gate (`src/bands/router-readback.rs:330-358`); PIN mint/readback (`src/bands/router-readback.rs:360-430`); logout invalidates token (`src/bands/router-readback.rs:433-445`). |
| B3 | `/api/admin/*` | `src/bands/full-rust-routes.rs:13-78`, `src/bands/router-readback.rs:353-358` | whole-route admin surface by contract; FIELD treats this as outside field audit and lawful only if the route/session gate is proven real, not as field projection | snapshot pull | admin routes are registered under `/api/admin/*` (`src/bands/full-rust-routes.rs:13-78`); the session membrane recognizes only authorized tokens as admin (`src/bands/router-readback.rs:353-358`); per-route gate verification remains the translator boundary for this class. |
| B4 | `/api/themes`, `/api/favorites`, `/api/get_starred_tab`, `/api/set_starred_tab`, `/api/tab-bar`, `/api/tabs`, `/api/tabs/:tab_id/manifest`, `/api/tabs/visibility`, `/api/tabs/elements`, `/api/tabs/elements/:tab_id`, `/api/stats/elements`, `/api/portals/elements` | `src/bands/runtime.rs:64-68`, `src/bands/runtime.rs:81-83`, `src/bands/full-rust-routes.rs:3-7`, `src/bands/routes.rs:152-260`, `src/bands/crown-law/element-fragments.rs:13-60` | projected through iris/session where tab/element visibility is in scope; admin mutations require admin session; fragment pulls construct guest/admin shapes | snapshot pull; pulse-riding pull for `tabs.changed` | tab-bar uses `session_from_headers()` (`src/bands/routes.rs:244-260`); visibility mutation refuses non-admin and pokes `TabsChanged` (`src/bands/routes.rs:216-235`); element visibility refuses non-admin and fragments are session-projected (`src/bands/crown-law/element-fragments.rs:13-60`). |
| B5 | `/api/stats`, `/api/stats/elements`, `/api/stats/events`, `/api/stats/events/renew` | `src/bands/runtime.rs:71-73`, `src/bands/routes.rs:87-93`, `src/bands/crown-law/element-fragments.rs:39-45` | ALREADY LAWFUL exemplar: session-projected construction; guest type cannot represent denied stats fields | pulse-riding pull via `stats.tick`; snapshot pull for the data route | route projection (`src/bands/routes.rs:87-93`); guest/admin structs and projector (`src/bands/contracts/core.rs:31-42`, `src/bands/contracts/core.rs:89-180`); pulse law (`docs/stats-data-plane-audit.md:107-110`). |
| B6 | `/api/status`, `/api/uptime`, `/api/status/internet/speedtest`, `/api/status/power/usage`, `/status/power/usage` | `src/bands/full-rust-routes.rs:80-81`, `src/bands/full-rust-routes.rs:115-117`, `src/bands/full-rust-routes.rs:352-407`, `src/bands/full-rust-routes/power.rs:1-38` | serves-admin-shape-to-guests for internet/power details; uptime is lifecycle aggregate | snapshot pull | internet status returns connectivity hosts and timestamp (`src/bands/full-rust-routes.rs:356-382`); uptime reads `/proc/uptime` (`src/bands/full-rust-routes.rs:388-407`); power returns current/history watts (`src/bands/full-rust-routes/power.rs:1-38`). |
| B7 | `/api/status/services`, `/api/services/data`, `/api/service/control` | `src/bands/full-rust-routes.rs:79`, `src/bands/runtime.rs:62`, `src/bands/full-rust-routes.rs:113`, `src/bands/crown-law/service-installer.rs:45-56` | services readbacks are whole current route/readback shape; denylist law still names `isEnabled` as admin field | snapshot pull | service monitor law names fields and `isEnabled` admin field (`src/bands/crown-law/service-installer.rs:45-49`); generic read route returns path/family/status (`src/bands/full-rust-routes.rs:425-453`). |
| B8 | `/api/status/tailscale`, `/api/status/tailscale/connect`, `/api/status/tailscale/authkey`, `/api/status/tailscale/disconnect`, `/api/status/tailscale/enable`, `/api/status/tailscale/disable`, `/api/status/tailscale/config`, `/api/status/tailscale/update-tailnet` | `src/bands/full-rust-routes.rs:82-89`, `src/bands/crown-law/service-installer.rs:50-52` | serves-admin-shape-to-guests or mutation readback; topic law still names identity/secret-ish fields with no allowlist projection | snapshot pull | tailscale topic law names status/interface plus admin fields `ip`, `tailnet`, `isEnabled`, `loginUrl` (`src/bands/crown-law/service-installer.rs:50-52`); routes are registered (`src/bands/full-rust-routes.rs:82-89`). |
| B9 | `/api/status/vpn/pia`, `/api/status/vpn/transmission`, `/api/status/vpn/updatekey/pia`, `/api/status/vpn/updatekey/transmission`, `/api/status/vpn/pia/exists`, `/api/status/vpn/transmission/exists`, `/api/status/vpn/enable`, `/api/status/vpn/disable`, `/api/status/vpn/check-enabled` | `src/bands/full-rust-routes.rs:90-98`, `src/bands/crown-law/service-installer.rs:52` | serves-admin-shape-to-guests or mutation readback; secret-class fields have no typed allowlist projection yet | snapshot pull | vpn topic law names `connectionDetails`, `credentials`, and `isEnabled` as admin fields (`src/bands/crown-law/service-installer.rs:52`); route registrations (`src/bands/full-rust-routes.rs:90-98`). |
| B10 | `/api/dhcp/status`, `/api/dhcp/leases`, `/api/dhcp/reservations`, `/api/dhcp/reservations/:reservation_id`, `/api/dhcp/config`, `/api/dhcp/health`, `/api/dhcp/statistics`, `/api/dhcp/pool-boundary`, `/api/kea-leases`, `/api/network/notes`, `/api/wakeonlan/targets`, `/api/wakeonlan/wake`, `/api/wakeonlan/targets/:name`, `/api/wakeonlan/status` | `src/bands/full-rust-routes.rs:118-132`, `src/bands/full-rust-routes.rs:121-124` | no field law yet; current generic readback exposes route/family, while intended domain carries LAN identities | snapshot pull | route registrations (`src/bands/full-rust-routes.rs:118-132`, `src/bands/full-rust-routes.rs:121-124`); generic network-control family classifier (`src/bands/full-rust-routes.rs:492-509`). |
| B11 | `/api/files/browse`, `/api/files/browse-hierarchical`, `/admit/upload/tree`, `/api/files/upload`, `/api/files/download`, `/api/upload/*` | `src/bands/full-rust-routes.rs:99-109`, `src/bands/runtime.rs:78`, `src/bands/full-rust-routes/upload.rs:27-83`, `src/bands/full-rust-routes.rs:242-310` | browse/tree currently expose filesystem display paths under upload root; upload mutation returns filename/destination/bytes | snapshot pull | upload root defaults to `/mnt/nas` and entries carry `name`, `path`, `type`, children flags (`src/bands/full-rust-routes/upload.rs:15-83`); upload submit returns filename, bytes, destination and Caduceus readback (`src/bands/full-rust-routes.rs:242-310`). |
| B12 | `/api/portals`, `/api/portals/:portal_name`, `/api/portals/factory`, `/api/portals/images/:filename`, `/api/service/control` | `src/bands/full-rust-routes.rs:110-114`, `src/bands/crown-law/element-fragments.rs:136-180` | portal fragments are session-projected by iris; portal JSON routes need field law for destination/service facts | snapshot pull | portal fragment uses plan grants and renders destination/status/services with admin controls only for admin (`src/bands/crown-law/element-fragments.rs:136-180`); route registrations (`src/bands/full-rust-routes.rs:110-114`). |
| B13 | `/api/youtube/*`, `/api/nasLinker/*`, `/api/backblazeTab/*`, `/api/backup/*`, `/api/miner/*`, `/api/test/*`, `/api/conflict/status` | `src/bands/full-rust-routes.rs:133-238` | guest-reachable generic module readbacks/mutations; no FIELD law yet; HYPOTHESIS for quarry-specific field bodies until runtime/plugin observation | snapshot pull | route registrations span these module families (`src/bands/full-rust-routes.rs:133-238`); generic read route returns path/family/status (`src/bands/full-rust-routes.rs:425-453`). |

## C. Field classification table for mixed-privilege facts

Conservative defaults used here: secret-class is `DENY-to-guest`; identity-class is `DENY-to-guest`; lifecycle/aggregate facts are `GUEST-VISIBLE` unless coupled to identity, privilege, or implementation detail. `system.stats` rows cite the already-ratified O1-O8 result and are exemplar, not open work.

| field / family | routes | fact-kind | conservative ruling | F-id / question | citation |
| --- | --- | --- | --- | --- | --- |
| `system.stats.schema`, `topic`, resource load/memory/swap, aggregate storage bytes, aggregate network bytes, guest telemetry currentness, `nextRoutes` | `/api/stats` | lifecycle | GUEST-VISIBLE | already ruled by O1-O8; no F-id | guest projector fields (`src/bands/contracts/core.rs:31-87`, `src/bands/contracts/core.rs:109-145`); PULSE-003 table (`docs/stats-data-plane-audit.md:52-80`, `docs/stats-data-plane-audit.md:114-125`). |
| `system.stats.doctrine`, `transport.streamReason`, `network.interfaces`, `network.connections`, `io`, `leases`, `processes`, `services`, raw `storage[].name/mount/source`, `telemetry.serviceHealth`, `telemetry.firstMissingSignal` | `/api/stats` | identity / lifecycle / privilege | DENY-to-guest where identity/admin detail; lawful already via projector | already ruled by O1-O8; no F-id | guest type omits these fields (`src/bands/contracts/core.rs:31-87`); admin type includes them (`src/bands/contracts/core.rs:89-180`); denial wall names denied fields (`src/bands/tests/stats-projection-walls.rs:181-204`). |
| `internet.status`, `timestamp`, connectivity result | `/api/status` | lifecycle | GUEST-VISIBLE | none | route returns `status` and `timestamp` (`src/bands/full-rust-routes.rs:370-382`); topic law carries status/timestamp (`src/bands/crown-law/service-installer.rs:50`). |
| `internet.hosts`, `timeoutSeconds`, authority strings | `/api/status` | identity / lifecycle | RULING-NEEDED | F1: Should guest internet status expose probe host identities or only connection state? | route returns hard-coded host identities (`src/bands/full-rust-routes.rs:356-382`). |
| `internet.publicIp`, `ipDetails`, `dnsServers` | internet topic; no current live field body found beyond contract | identity | DENY-to-guest | FIELD-002 should confirm live route body or mark dead config | denylist genome (`src/bands/crown-law/registry-startup.rs:441-449`); topic law (`src/bands/crown-law/service-installer.rs:50`). |
| `vpn.credentials`, keys/updatekey material | `/api/status/vpn/*` | secret | DENY-to-guest | none; secret-class terminal | denylist genome (`src/bands/crown-law/registry-startup.rs:450-456`); vpn routes (`src/bands/full-rust-routes.rs:90-98`). |
| `vpn.connectionDetails` | `/api/status/vpn/*` | identity | DENY-to-guest | F2: Is any guest VPN connection summary needed after secret fields are split? | denylist genome (`src/bands/crown-law/registry-startup.rs:450-456`); topic law (`src/bands/crown-law/service-installer.rs:52`). |
| `vpn.vpnStatus`, `transmissionStatus`, `timestamp` | `/api/status/vpn/*` | lifecycle | GUEST-VISIBLE | none if constructed without credentials/details | topic law fields (`src/bands/crown-law/service-installer.rs:52`). |
| `vpn.isEnabled` | `/api/status/vpn/check-enabled`, `/api/status/vpn/enable`, `/api/status/vpn/disable` | lifecycle / privilege | RULING-NEEDED | F3: Is enablement a lifecycle indicator guests may see, or an admin capability fact? | denylist genome includes services-like `isEnabled` pressure (`src/bands/crown-law/registry-startup.rs:450-456`); routes (`src/bands/full-rust-routes.rs:96-98`). |
| `tailscale.ip`, `tailnet`, `loginUrl` | `/api/status/tailscale*` | identity / secret-adjacent | DENY-to-guest | F4: May any transformed tailnet label be displayed to guests, or only connected/disconnected? | denylist genome (`src/bands/crown-law/registry-startup.rs:464-470`); topic law (`src/bands/crown-law/service-installer.rs:51`). |
| `tailscale.status`, `interface`, `timestamp` | `/api/status/tailscale` | lifecycle / identity | RULING-NEEDED | F5: Does guest need raw interface names, or only safe connection state? | topic law (`src/bands/crown-law/service-installer.rs:51`). |
| `tailscale.isEnabled` | `/api/status/tailscale/enable`, `/api/status/tailscale/disable`, `/api/status/tailscale/config` | lifecycle / privilege | RULING-NEEDED | F6: Same enablement ruling as VPN/services or separate Tailscale law? | denylist genome (`src/bands/crown-law/registry-startup.rs:464-470`). |
| `services.name`, `status`, `statusDetails`, `isActive`, `needsReboot`, `port`, `systemdName`, `isScriptManaged` | `/api/status/services`, `/api/services/data`, `/api/service/control` | identity / lifecycle | RULING-NEEDED | F7: Which service identifiers and status fields are guest appliance currentness versus admin diagnostics? | service fields (`src/bands/crown-law/service-installer.rs:45-49`). |
| `services.isEnabled` | `/api/status/services`, `/api/services/data` | lifecycle / privilege | RULING-NEEDED | F8: Treat enable-at-boot as guest lifecycle, or admin capability/privilege? | denylist genome (`src/bands/crown-law/registry-startup.rs:471-474`); service topic law (`src/bands/crown-law/service-installer.rs:45-49`). |
| DHCP/Kea `hostname`, `ip`, `mac`, lease identities, reservation identities | `/api/dhcp/*`, `/api/kea-leases`, stats `leases[]` | identity | DENY-to-guest | FIELD-003 should build the DHCP allowlist | current stats lease schema (`src/bands/contracts/core.rs:129-136`); Kea collector (`src/bands/crown-law/stats-tabbar.rs:534-570`); DHCP routes (`src/bands/full-rust-routes.rs:125-132`). |
| DHCP `statistics`, `health`, pool aggregate counts | `/api/dhcp/statistics`, `/api/dhcp/health`, `/api/dhcp/status` | lifecycle | GUEST-VISIBLE if aggregate-only | F9: Which DHCP aggregate health fields may guest see? | route registrations (`src/bands/full-rust-routes.rs:125-132`); current generic readback is HYPOTHESIS for final payload (`src/bands/full-rust-routes.rs:425-453`). |
| Wake-on-LAN target `name`, target MAC/IP, status | `/api/wakeonlan/*` | identity / lifecycle | DENY-to-guest for target identity; aggregate status needs ruling | F10: Is any guest WOL target list visible, or admin-only? | WOL routes (`src/bands/full-rust-routes.rs:121-124`); network-control classifier (`src/bands/full-rust-routes.rs:492-509`). |
| Upload/default directory `/mnt/nas`, browsed directory `name/path/type`, filename/destination/bytes | `/api/files/*`, `/api/upload/*`, `/admit/upload/tree` | identity / lifecycle | DENY-to-guest for filesystem paths; lifecycle upload history/status may be guest-visible | F11: Should guest browse paths be replaced by product labels before display? | upload root and entry fields (`src/bands/full-rust-routes/upload.rs:15-83`); upload submit fields (`src/bands/full-rust-routes.rs:242-310`). |
| Portal `name`, `local_url`, `remote_url`, `description`, `status`, `services` | `/api/portals*`, `/api/portals/elements` | identity / lifecycle / preference | RULING-NEEDED | F12: Which portal destinations are guest-visible product links versus admin topology? | portal render carries destination/status/services (`src/bands/crown-law/element-fragments.rs:159-180`). |
| Theme `global.theme.name`, tokens, available themes | `/api/themes` | preference | GUEST-VISIBLE | none | theme source and default selection (`src/bands/routes.rs:434-469`). |
| Favorite/starred tab and visible non-admin tab ids/display names | `/api/favorites`, `/api/get_starred_tab`, `/api/tab-bar` | preference / identity | GUEST-VISIBLE after iris projection | none | favorite manifest reads homeserver tabs but validates visible non-admin starred tab (`src/bands/routes.rs:291-350`); tab-bar projection uses session (`src/bands/routes.rs:244-260`). |
| Generic module readback `schema`, `ok`, `success`, `method`, `path`, `family`, `status`, `authority`, `firstMissingSignal` | `/api/youtube/*`, `/api/nasLinker/*`, `/api/backblazeTab/*`, `/api/backup/*`, `/api/miner/*`, `/api/test/*`, `/api/conflict/status` | lifecycle / identity | RULING-NEEDED | F13: Should generic route/path/family readback be guest-visible or hidden behind admin/module projection? | generic readback shape (`src/bands/full-rust-routes.rs:425-453`); route families (`src/bands/full-rust-routes.rs:492-509`). |
| Backup/keyman credential route families | `/api/backup/keyman/*`, `/api/backup/providers/*/config`, `/api/backup/key` | secret / identity | DENY-to-guest | none; secret-class terminal | route registrations (`src/bands/full-rust-routes.rs:156-184`); generic route shape currently HYPOTHESIS for future payload (`src/bands/full-rust-routes.rs:425-453`). |

## D. Death map for `admin_field_filters()`

| denylist topic | genome fields | where facts flow today | FIELD tranche that retires it | citation |
| --- | --- | --- | --- | --- |
| `internet_status` | `publicIp`, `ipDetails`, `dnsServers` | No current live Rust body for these fields found; `/api/status` currently returns connectivity status, timestamp, probe `hosts`, timeout, authority. Treat `publicIp/ipDetails/dnsServers` as dead config until FIELD-002 confirms a route body. | FIELD-002 | genome (`src/bands/crown-law/registry-startup.rs:441-449`); live internet handler (`src/bands/full-rust-routes.rs:352-382`). |
| `vpn_status` | `connectionDetails`, `credentials` | Contract law names these fields; `/api/status/vpn/*` routes are registered through generic read/mutation membranes, with secret-class routes such as updatekey present. | FIELD-001 | genome (`src/bands/crown-law/registry-startup.rs:450-456`); routes (`src/bands/full-rust-routes.rs:90-98`); topic law (`src/bands/crown-law/service-installer.rs:52`). |
| `system_stats` | `processes`, `users`, `networkConnections` | Already lawful via PULSE-003c/d/e: guest `/api/stats` is constructed from `SystemStatsGuestProjection` and cannot represent processes/leases/interfaces/connections; admin projection carries full fields. No FIELD implementation open. | none; exemplar row only | route selection (`src/bands/routes.rs:87-93`); guest/admin projection structs (`src/bands/contracts/core.rs:31-107`); projector (`src/bands/contracts/core.rs:109-180`). |
| `tailscale_status` | `ip`, `tailnet`, `isEnabled`, `loginUrl` | Contract law names these fields; `/api/status/tailscale*` routes exist as generic read/mutation membranes; no typed allowlist projection yet. | FIELD-003 | genome (`src/bands/crown-law/registry-startup.rs:464-470`); routes (`src/bands/full-rust-routes.rs:82-89`); topic law (`src/bands/crown-law/service-installer.rs:51`). |
| `services_status` | `isEnabled` | Services law names full service rows and `isEnabled` as admin field; `/api/status/services` and `/api/services/data` are readback routes, `/api/service/control` is mutation. | FIELD-004 | genome (`src/bands/crown-law/registry-startup.rs:471-474`); routes (`src/bands/full-rust-routes.rs:79`, `src/bands/runtime.rs:62`, `src/bands/full-rust-routes.rs:113`); service law (`src/bands/crown-law/service-installer.rs:45-49`). |

Og quarry death exhibit: Flask registered admin fields at top level only (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:487-492`); non-admin filtering copied the payload and deleted only matching top-level fields (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/broadcasts/events.py:249-295`); `collect_system_stats()` placed `top_processes` under `cpu.top_processes` (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:356-370`), and each process row carried process identity and executable paths (`/fulcrum/attachments/homeserver/initialization/flask/inject/backend/stats/utils.py:445-484`). That nested leak is approved divergence and SHALL NOT port (`docs/pulse-substrate-audit.md:67-75`).

## E. Tranche ladder proposal

| tranche | topic | blast radius | work |
| --- | --- | --- | --- |
| FIELD-001 | `vpn_status` | highest: secret-class `credentials` and update-key surfaces | Build typed VPN projection: guest status only, admin detail/credentials separated; wall credentials/key/updatekey never appear in guest construction. |
| FIELD-002 | `internet_status` | identity-class public IP/DNS/probe topology | Build typed internet projection: guest connectivity state, admin network identity. Confirm whether `publicIp/ipDetails/dnsServers` are dead config or live in a route body. |
| FIELD-003 | `tailscale_status` plus DHCP/Kea/WOL identity surfaces | identity-class tailnet/IP/MAC/hostname/target inventory | Build typed network identity projection: guest connection/health aggregates, admin tailnet/IP/MAC/lease/target identities. |
| FIELD-004 | `services_status` | lifecycle/privilege ambiguity | Build typed services projection: guest appliance currentness, admin service identifiers, systemd names, ports, enablement if ruled privileged. |
| FIELD-005 | `upload`, `portals`, generic module route readbacks | broad guest topology/readback cleanup | Replace raw path/route/family topology with product labels or admin-only route facts where F11-F13 settle. |
| FIELD-006 | denylist terminal | low code blast after allowlists stand | Delete `admin_field_filters()` and add a negative wall proving no denylist/filter-after-construction mechanism exists. |

## F. Mechanical counts

- Route census rows: 13.
- Guest-reachable route patterns audited from the live route table: 267 distinct `/api/*` route patterns seen; 67 are `/api/admin/*` and classed as one whole-route admin row; 200 distinct non-admin `/api/*` route patterns remain in the guest-reachable audit set. Source: `src/bands/full-rust-routes.rs:1-238`, `src/bands/runtime.rs:40-83`.
- Field rows classified in section C: 23.
- Fact-kind row counts: identity-bearing rows 14; lifecycle-bearing rows 15; privilege-bearing rows 4; preference-bearing rows 3; secret-bearing rows 3. Rows can carry more than one kind.
- `DENY-to-guest` rows: 9.
- `RULING-NEEDED` cells: 8; F-question ids minted: F1-F13.
- Already-lawful exemplar rows: 2 (`system.stats` guest allowlist and denied admin-detail row).
- HYPOTHESIS markers: 3 (B13 generic module route bodies; C:DHCP-aggregates(F9); C:backup-keyman).
